link_libraries(
	miro
)

set( TARGETS
  multiple_servers
)

#foreach( TARGET ${TARGETS} )
#	add_executable( ${TARGET} 
#		${TARGET}.cpp
#	)
#endforeach( TARGET ${TARGETS} )
#
#install_targets(${TESTS_BIN_DIR}
#  ${TARGETS}
#)
